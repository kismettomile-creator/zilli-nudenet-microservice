# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Redis cache (optional)
redis==5.0.1

# HTTP client for external API calls
httpx==0.25.2

# NudeNet and image processing dependencies
nudenet==3.4.2
Pillow==10.2.0
opencv-python-headless==4.9.0.80
opencv-contrib-python-headless==4.9.0.80
numpy==1.26.4

# Age detection for child safety (18+ enforcement)
deepface==0.0.92
tensorflow==2.16.1
tf-keras==2.16.0

# InsightFace - 2. bağımsız yaş tahmin kaynağı (çift SDK doğrulama için, DeepFace'ten farklı mimari/ONNX tabanlı)
# Model ağırlıkları repo'ya commitlenmiyor, ilk çağrıda otomatik indiriliyor (~/.insightface)
insightface==0.7.3
# ⚠️ onnxruntime>=1.18 ZORUNLU: 1.17.x sadece ONNX IR version 9'a kadar destekliyor, ama
# nudenet'in kendi model dosyası (320n.onnx) IR version 10 ile export edilmiş - 1.17.1 pini
# NudeNet'in TAMAMEN yüklenememesine sebep oldu (production'da doğrulandı, model load hatası)
onnxruntime==1.19.2
# onnx pin ZORUNLU: pinlenmezse insightface en yeni onnx'i çeker, o da tensorflow==2.16.1'in
# gerektirdiği ml_dtypes~0.3.x ile çakışıp import hatası veriyor (yerelde doğrulandı)
onnx==1.16.1

# YOLO for person detection (gender=1 için)
ultralytics==8.3.0

# Falconsai NSFW classifier - ikinci/bağımsız nudity kontrol kaynağı (sadece "high" sensitivity)
transformers==4.44.2

# OpenAI Moderation API - üçüncü/bağımsız nudity kontrol kaynağı (ücretsiz endpoint, sadece "high" sensitivity)
openai==1.51.0

# Async utilities
asyncio-mqtt==0.16.1

# Logging and monitoring
python-multipart==0.0.6

# JSON and datetime utilities
python-dateutil==2.8.2

# Development dependencies (optional)
pytest==7.4.3
pytest-asyncio==0.21.1
